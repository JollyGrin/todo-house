{
  "name": "todo-house",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd apps/web && npm run dev",
    "build": "cd apps/web && npm install && npm run build",
    "start": "cd apps/web && npm start",
    "lint": "cd apps/web && npm run lint",
    "install:apps": "cd apps/web && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}